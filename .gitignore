# Zig 构建产物
.zig-cache/
.zig-tmp-cache/
zig-out/

# 本地索引 / 本机跑 agent 留下的目录
.code-abyss/
.pi/

# 编辑器 / 系统
*.swp
*~
.DS_Store

# 本地配置与凭证。piz 的配置在 ~/.piz/,不该出现在仓库里,
# 但防手滑:models.json 与 auth.json 含 apiKey。
.env
.env.*
auth.json
models.json
*.pem
*.key
